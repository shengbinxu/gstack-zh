# `/unfreeze` 技能深度注解

> 对应源文件：[`unfreeze/SKILL.md.tmpl`](https://github.com/garrytan/gstack/blob/main/unfreeze/SKILL.md.tmpl)
> 解除 /freeze 的编辑限制。

---

## 功能

删除 `~/.gstack/freeze-dir.txt`，恢复全目录编辑权限。一行命令。

**不需要参数**——直接解除当前 freeze。

和 `/freeze` 配对使用。调试完成或需要编辑范围外文件时运行。
